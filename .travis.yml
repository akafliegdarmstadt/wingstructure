language: python
dist: xenial
python:
  - "3.6"
install:
  - pip install .
  - pip install python-coveralls
  - pip install pytest-cov
script:
  - pytest tests --cov=wingstructure
after_success:
  - coveralls
